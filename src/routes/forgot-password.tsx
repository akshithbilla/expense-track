import { Link, createFileRoute } from "@tanstack/react-router";
import { useState } from "react"; import { toast } from "sonner"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { requestPasswordReset } from "@/lib/server-auth"; import { AuthFrame, Field } from "./login";
export const Route = createFileRoute("/forgot-password")({ component: ForgotPassword });
function ForgotPassword() { const [email, setEmail] = useState(""); const [sent, setSent] = useState(false); const submit = async (e: React.FormEvent) => { e.preventDefault(); try { await requestPasswordReset({ data: { email } }); setSent(true); toast.success("If the account exists, reset instructions are on their way."); } catch { toast.error("Unable to request a reset. Please try again."); } }; return <AuthFrame title="Reset your password" subtitle="Enter your email and we’ll send a reset link.">{sent ? <p className="rounded-xl bg-accent p-4 text-sm">Check your inbox for reset instructions. You can safely close this page.</p> : <form onSubmit={submit} className="grid gap-4"><Field label="Email"><Input type="email" value={email} onChange={(e) => setEmail(e.target.value)} required /></Field><Button>Send reset link</Button></form>}<p className="mt-6 text-center text-sm"><Link to="/login" className="text-primary hover:underline">Back to sign in</Link></p></AuthFrame> }
