import { Link, createFileRoute } from "@tanstack/react-router";
import { ArrowRight, BarChart3, LockKeyhole, WalletCards } from "lucide-react";
import { Button } from "@/components/ui/button";
export const Route = createFileRoute("/")({ component: Landing });
function Landing() { return <main className="min-h-screen bg-background"><nav className="mx-auto flex max-w-6xl items-center justify-between px-6 py-5"><div className="text-lg font-bold">Spendly</div><div className="flex gap-2"><Button variant="ghost" asChild><Link to="/login">Sign in</Link></Button><Button asChild><Link to="/signup">Get started</Link></Button></div></nav><section className="mx-auto grid max-w-6xl gap-12 px-6 py-20 lg:grid-cols-2 lg:py-28"><div><p className="mb-4 text-sm font-semibold text-primary">PRIVATE PERSONAL FINANCE</p><h1 className="font-display text-5xl tracking-tight sm:text-7xl">Know where every rupee goes.</h1><p className="mt-6 max-w-xl text-lg leading-8 text-muted-foreground">A calm, secure home for your expenses, budgets, and spending insights — available wherever you are.</p><div className="mt-8 flex gap-3"><Button size="lg" asChild><Link to="/signup">Create your account <ArrowRight className="ml-2 h-4 w-4" /></Link></Button><Button size="lg" variant="outline" asChild><Link to="/login">Sign in</Link></Button></div></div><div className="grid gap-4 rounded-3xl border bg-card p-6 shadow-elegant"><Feature icon={<WalletCards />} title="One clear picture" text="Track expenses with categories, payment methods and notes."/><Feature icon={<BarChart3 />} title="Useful insights" text="See trends, category breakdowns and monthly budget progress."/><Feature icon={<LockKeyhole />} title="Private by default" text="Your data is tied to your secure account and stored in MongoDB."/></div></section></main> }
function Feature({ icon, title, text }: { icon: React.ReactNode; title: string; text: string }) { return <div className="flex gap-4 rounded-2xl bg-accent/50 p-5"><div className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-primary text-primary-foreground">{icon}</div><div><h2 className="font-semibold">{title}</h2><p className="mt-1 text-sm text-muted-foreground">{text}</p></div></div> }
